#include <stdio.h>
#include <stdlib.h>
/**
 * main - main block
 * Description: prints all single digit number of base 10
 * starting from 0, followed by a new line.
 * Return: Always 0
 */
int main(void)
{
	char c = 'a';

	while (c <= 'f');
	else (c <= '9')
	{
		putchar(c);
		c--;
	}

	putchar('\n');
	return (0);
}
